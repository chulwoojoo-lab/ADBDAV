# ABDAV 컨텍스트 노트

## 배경
Mac은 MTP를 지원하지 않아 안드로이드 폰이 Finder에 안 붙는다.
MacDroid는 File Provider 방식이라 파일을 미리 전부 복사해두어 느리고 디스크를 먹었다.
이미지 캡처(PTP)는 사진만 보여주고 폴더 정리가 안 된다.

## 채택한 방식
폰에 rclone 바이너리를 넣고 WebDAV 서버를 띄운 뒤,
adb forward로 USB 터널을 만들어 Finder가 WebDAV로 마운트한다.

측정치 (2026-09-10)
- 폴더 열기 169개 항목: 0.13초
- 폰으로 쓰기: 232 MB/s
- 사진 1장(4MB) 열기: 0.04초

## 결정과 이유

### 왜 WebDAV인가
Finder가 알아듣는 프로토콜은 FTP, WebDAV, SMB 뿐이다.
FTP는 macOS에서 읽기 전용이라 폴더 정리가 안 된다.
SMB 서버는 안드로이드에서 구현이 까다롭다.
WebDAV만 읽기/쓰기가 모두 되면서 구현이 간단하다.

### 왜 adb forward인가
Wi-Fi를 타면 느리고 폰 IP가 바뀌면 끊긴다.
adb forward는 USB로 터널을 뚫어 속도가 나오고 IP와 무관하다.
서버를 127.0.0.1에만 바인딩해서 같은 Wi-Fi의 다른 기기에 노출되지 않는다.

### 왜 rclone인가
폰에 앱을 설치하지 않고 바이너리 하나만 넣었다 빼면 된다.
정적 링크된 arm64 빌드라 의존성이 없다.
/data/local/tmp 에서 adb shell(uid 2000)로 실행 가능함을 확인했다.

### 왜 macFUSE를 안 쓰는가
Apple M5 + SIP 활성 환경에서 커널 확장을 쓰려면 보안 설정을 낮춰야 한다.
파일 브라우징 목적으로는 과한 대가다.

### 왜 Xcode 없이 빌드하는가
Command Line Tools의 swiftc 6.3.1과 macOS 26 SDK로 AppKit 앱을 빌드할 수 있다.
Xcode 전체(수십 GB) 설치가 불필요하다.

## 알려진 제약
- 폰 화면이 잠겨 있으면 안드로이드가 저장소를 노출하지 않는다.
- Finder 사이드바 이름이 마운트 주소로 표시된다.
- 서버 프로세스는 USB를 뽑거나 폰을 재부팅하면 죽는다. 앱이 재시작한다.
- rclone 바이너리는 /data/local/tmp 에 남는다. 재부팅해도 유지된다.

## 앱 구현 (2026-09-10)

### 구조
Swift + AppKit 메뉴바 앱. Xcode 없이 Command Line Tools swiftc 6.3.1로 빌드한다.
`build.sh` 하나로 컴파일, 번들 생성, rclone 동봉, 임시 서명까지 끝난다.

### 셸 스크립트와의 차이
`~/bin/phone.sh` 는 그대로 두었다. 앱이 안 될 때 쓸 수 있는 대비책이다.
앱은 스크립트에 없던 것을 추가했다.
- 잠금 화면 감지 후 안내
- rclone이 폰에 없으면 앱 번들에서 자동 배포
- 꽂으면 자동 연결
- 앱 종료 시 자동 정리

### 미검증 변경 하나
Finder 볼륨 이름이 `127.0.0.1` 로 뜨는 게 보기 싫어서
rclone의 `--baseurl /ABDAV` 를 붙이고 그 주소로 마운트하도록 했다.
macOS가 WebDAV 볼륨 이름을 URL 마지막 경로에서 따온다는 가정인데 아직 확인하지 못했다.
폰 연결 후 확인이 필요하다. 안 되면 baseurl을 빼고 `/Volumes/127.0.0.1` 로 되돌린다.

### 왜 폴링인가
USB 연결 감지에 IOKit 알림을 쓰면 코드가 늘어난다.
3초 폴링으로 충분히 반응이 빠르고 CPU도 거의 안 쓴다.

## 실전 테스트에서 잡은 버그 (2026-09-11)

### pgrep 자기 자신 매칭
폰에서 서버가 이미 도는지 `pgrep -f rclone` 으로 확인했는데,
`-f` 는 명령줄 전체를 훑기 때문에 그 확인 명령 자체에 들어 있는 "rclone" 글자를 잡아냈다.
그래서 항상 "이미 실행 중"으로 판단해 서버를 띄우지 않았고,
터널만 만들어진 채 마운트가 실패했다.

`pgrep -x` 는 프로세스 이름만 정확히 비교하므로 오탐이 없다. pkill 도 같이 고쳤다.
셸 스크립트 쪽은 `ps -A | grep "[r]clone"` 대괄호 기법을 써서 원래 안전했다.

교훈. 프로세스 존재 확인에 `-f` 를 쓸 때는 확인 명령 자신이 걸리는지 반드시 따져본다.

### 검증된 것
- 꽂고 앱 실행하면 10초 안에 자동 마운트
- 볼륨 이름이 /Volumes/ABDAV 로 제대로 나옴 (baseurl 방식이 통했다)
- 폴더 생성, 파일 쓰기, 읽기, 삭제 모두 정상
- 사진 170장 폴더 여는 데 0.03초
- 폰에서 rclone 을 지워도 앱이 15초 안에 다시 배포함

## 연결 모드 전환 추가 (2026-09-11)

메뉴에서 USB와 Wi-Fi를 고르게 했다. 같은 adb를 쓰므로 코드 구조만 바꾸면 됐다.
모든 adb 명령에 `-s <시리얼>` 을 붙여 대상 기기를 명시한다.
시리얼에 콜론이 있으면 무선(예: 10.1.4.33:43783), 없으면 USB다.

Wi-Fi 모드에서 기기가 안 붙어 있으면 `adb mdns services` 로 광고를 찾아
`adb connect` 를 시도한다. 페어링은 사용자가 폰에서 미리 해둬야 한다.

### 실측 속도 차이 (2026-09-11, 사내망)
| 항목 | USB | Wi-Fi |
|---|---|---|
| 폴더 열기(170장) | 0.03초 | 0.11초 |
| 사진 1장(4MB) | 0.04초 | 0.55초 |
| 쓰기 | 232 MB/s | 6.4 MB/s |
| 순수 adb 전송 | 273 MB/s | 8.3 MB/s |

무선이 느린 건 우리 구조 탓이 아니다. WebDAV를 안 거친 adb 자체가 8.3 MB/s다.
폰과 Mac이 서로 다른 AP에 붙어 사내망을 한 바퀴 도는 데다
무선 adb는 TLS 암호화를 거친다. 둘 중 어느 쪽이 얼마나 기여하는지는 못 갈랐다.

## 로그로 잡은 버그 (2026-09-11)

Wi-Fi 모드가 안 붙길래 추측하다 시간을 버렸다. 로그를 넣으니 한 줄로 나왔다.
원인은 폰 화면이 잠겨 있던 것이었다. 안드로이드는 잠금 상태에서 저장소를 안 내준다.

그 과정에서 진짜 버그 두 개를 고쳤다.
- 자동 연결이 실패할 때마다 경고창을 띄웠다. 3초마다 재시도하므로 창이 계속 떴을 것이다.
  이제 자동 실패는 조용히 넘기고 메뉴 상태로만 알린다. 수동 연결만 창을 띄운다.
- 실패 직후 곧바로 재시도해 폰을 두들겼다. 30초 쉬었다 다시 시도하도록 했다.

진단 로그는 ~/Library/Logs/ABDAV.log 에 남는다. 상태가 바뀔 때만 쓴다.

## 에러 메시지 구체화 (2026-09-11)

"연결할 수 없습니다" 같은 말은 쓸모가 없다. 실제로 이번 세션에서
USB 디버깅이 꺼진 걸 알아내는 데 손으로 ioreg 를 뒤져야 했다. 그 진단을 앱에 넣었다.

### 어떻게 구분하는가
adb 가 기기를 못 볼 때 macOS 의 USB 장치 목록을 직접 본다.
- adb 전용 인터페이스는 클래스 255, 서브클래스 66 이다. 이게 있으면 폰은 준비된 것이다.
- 그게 없는데 안드로이드 제조사 이름의 USB 기기가 보이면 USB 디버깅이 꺼진 것이다.
- 아무것도 없으면 케이블이 안 꽂힌 것이다.

`adb devices` 의 상태도 본다. device 말고 unauthorized(폰에서 허용 안 누름),
offline(응답 없음) 이 오는데 각각 다른 안내를 한다.

### 곁다리로 고친 것
연결이 끊기는 순간 `getprop` 이 "error: closed" 를 돌려주는데
그걸 기기 이름으로 그대로 표시해 "error: closed 연결됨" 이라는 문구가 떴다.
오류 문자열이 섞이면 "Android" 로 대체한다.

## 잘못 넣었던 잠금 검사 제거 (2026-09-11)

연결 전에 폰 화면이 잠겼는지 보고 잠겼으면 거부했다. 이게 틀렸다.

초반에 이미지 캡처(PTP)로 시도하다 잠금 때문에 막힌 경험을 그대로 가져왔는데,
PTP 는 안드로이드가 미디어를 노출하는 방식이라 잠금이 막지만
우리 방식은 adb 셸이 파일시스템을 직접 읽으므로 잠금과 무관하다.

잠긴 폰에서 직접 확인했다. 목록 조회, 파일 읽기, 파일 쓰기 모두 정상 동작한다.
검사를 걷어내니 잠긴 상태에서도 3초 만에 자동 연결된다.

교훈. 다른 경로에서 겪은 제약을 근거 없이 새 경로에 옮겨 붙이지 않는다.

## 실측 속도 (2026-09-11, USB)
| 조건 | 속도 |
|---|---|
| 큰 파일 하나 (200MB) | 232 MB/s |
| 작은 파일 여럿 (40개, 93MB) | 107 MB/s, 개당 0.021초 |
| 참고: Mac 내장 SSD (dd) | 1,132 MB/s |

작은 파일이 느린 건 WebDAV 가 파일마다 요청을 주고받기 때문이다.
사진 정리 용도에는 충분하다. 45GB 를 옮겨도 7분 정도다.

## 껍데기 마운트 문제 (2026-09-11)

케이블을 뽑았다 꽂으면 adb 가 터널을 버린다. 그런데 macOS 의 마운트 표에는
/Volumes/ABDAV 항목이 그대로 남는다. 접근하면 타임아웃이 나는 죽은 마운트다.

앱은 `mount` 출력에 항목이 있으면 연결된 것으로 판단했기 때문에
이 껍데기를 보고 "연결됨" 이라고 표시하며 영영 다시 붙지 않았다.
사용자 입장에서는 Finder 에서 폴더가 안 열리는데 앱은 멀쩡하다고 우기는 꼴이다.

### 고친 방법
마운트 항목이 있는지만 보지 않고, 터널에 실제로 WebDAV 요청을 보내 응답을 확인한다.
localhost 요청이라 비용이 거의 없고, 터널과 폰 서버가 모두 살아 있어야 2xx 가 온다.
응답이 없으면 껍데기로 판단해 강제 언마운트하고 정상 연결 절차를 다시 태운다.

터널만 끊어 재현한 결과 8초 만에 스스로 회복했다.

교훈. "있는가" 와 "동작하는가" 는 다르다. 상태 판단은 흔적이 아니라 응답으로 한다.

## 이름 변경 (2026-09-11)

PhoneLink 에서 ABDAV 로 바꿨다. adb 와 WebDAV 를 합친 이름이다.
문서의 옛 이름도 전부 ABDAV 로 바꿨다. 혼자 쓰는 기록이라 일관성이 낫다고 봤다.

같이 바뀐 것들.
- Finder 볼륨 이름: /Volumes/ABDAV
- 번들 ID: local.abdav
- 로그 파일: ~/Library/Logs/ABDAV.log
- 설정 저장 위치도 번들 ID 를 따르므로 연결 모드 설정이 초기화된다

## macOS 네트워크 볼륨 권한 (2026-09-11)

앱이 마운트에 접근할 때 macOS 가 한 번 물어본다. 허용하면 유지된다.
다만 임시 서명(adhoc)이라 코드가 바뀌면 서명 해시가 달라지고,
macOS 는 그걸 다른 앱으로 보아 다시 묻는다. 직접 확인했다.

| 상황 | 서명 해시 |
|---|---|
| 그대로 재빌드 | 동일 (주석만 바꿔도 동일. 컴파일 결과가 같다) |
| 코드 변경 후 빌드 | 달라짐 |

즉 개발이 끝나 더 이상 빌드하지 않으면 권한 창도 더는 안 뜬다.
영구적으로 없애려면 Apple 개발자 계정(연 99달러)으로 정식 서명해야 한다.

Info.plist 에 NSNetworkVolumesUsageDescription 을 넣어 권한 창에
왜 필요한지 한국어로 설명되게 했다. 창 자체를 없애지는 못한다.

## 이름 바꾸다 드러난 버그 (2026-09-11)

이름을 바꾸고 첫 실행에서 마운트가 -5014 오류로 실패했다.
폰에서 돌던 옛 서버가 `--baseurl /PhoneLink` 로 서비스하고 있었는데,
앱은 "rclone 이 떠 있으면 그냥 쓴다" 는 식이라 그걸 재사용했다.
새 주소인 /ABDAV 는 404 였다.

이름 변경이 아니어도 포트를 바꾸거나 공유 경로를 바꾸면 똑같이 터졌을 문제다.
이제 도는 서버의 명령줄을 읽어 현재 설정과 맞는지 확인하고,
다르면 죽이고 새로 띄운다. 옛 서버를 일부러 남겨둔 채 재현 테스트를 했고
10초 만에 교체하고 연결했다.

교훈. 떠 있다고 쓸 수 있는 게 아니다. 내가 원하는 설정으로 떠 있는지까지 봐야 한다.

## Finder 에서 두 단계 들어가던 문제 (2026-09-11)

사이드바 위치에 127.0.0.1 이 뜨고 그 안에 ABDAV 가 들어 있어 두 번 눌러야 했다.

원인은 마운트 방식이었다. AppleScript 의 `mount volume` 은 NetFS 를 거치면서
Finder 에 "서버 연결" 로 등록된다. Finder 는 그런 마운트를 서버 항목 아래
공유 목록으로 묶어 보여준다. 그래서 호스트 이름인 127.0.0.1 이 겉으로 드러났다.

### 고친 방법
`mount_webdav` 를 직접 호출하고, 마운트 지점을 홈 폴더의 ~/ABDAV 로 잡았다.
서버 등록이 일어나지 않아 127.0.0.1 항목이 아예 생기지 않고,
Finder 는 ABDAV 라는 볼륨 하나로만 인식한다. 홈 폴더에도 바로 보인다.

볼륨 이름은 `-v` 옵션이 아니라 **마운트 지점 폴더 이름**을 따라간다.
숨김 폴더(.abdav_mnt)에 걸었더니 볼륨 이름도 .abdav_mnt 가 됐다.
그래서 폴더 이름을 ABDAV 로 둬야 한다.

/Volumes 아래에는 root 권한이 없어 폴더를 못 만든다. 홈 폴더를 쓴 이유다.
연결이 끊기면 ~/ABDAV 는 빈 폴더로 남는다. 마운트 지점이 고정되는 이점이 있다.

`-S` 옵션도 같이 넣었다. 인증창을 막고, 서버가 응답을 멈추면 즉시 언마운트해
껍데기 마운트가 남는 것을 줄여준다.

## 사이드바 이름표 (2026-09-11)

Finder 사이드바 위치 항목의 이름은 볼륨 이름이 아니라
**마운트 주소의 호스트 이름**을 그대로 따라간다. 화면 캡처로 확인했다.

| 마운트 주소 | 사이드바 표시 |
|---|---|
| http://127.0.0.1:8090/ABDAV | 127.0.0.1 |
| http://abdav.localhost:8090/ABDAV | abdav.localhost |
| http://ABDAV.localhost:8090/ABDAV | ABDAV.localhost (대소문자 유지됨) |

`mount_webdav -v` 로 주는 볼륨 이름은 창 제목과 Finder 디스크 목록에만 반영되고
사이드바 이름표는 바꾸지 못한다.

### 대응
`ABDAV.localhost` 로 마운트한다. `*.localhost` 는 어느 Mac 에서나
설정 없이 127.0.0.1 로 풀리므로 아무 준비 없이 동작한다.

접미사 없이 ABDAV 로만 띄우려면 /etc/hosts 에 항목을 넣어야 하는데,
쓰는 사람마다 시스템 파일을 고치게 하는 건 배포 가능한 방식이 아니라 버렸다.
후보 목록에 남겨두면 매번 실패하는 이름을 먼저 시도하느라
연결이 10초까지 늘어나기도 했다. 빼고 나니 3초로 돌아왔다.

사이드바 즐겨찾기에 앱이 직접 등록하는 방법도 시험했다.
LSSharedFileList 는 10.11 에서 deprecated 됐고 macOS 26 에서는
호출하는 즉시 세그먼테이션 오류로 죽는다. 대체 공개 API 가 없다.
정확한 이름을 원하면 사용자가 ~/ABDAV 를 즐겨찾기로 한 번 끌어다 놓으면 된다.

마운트 지점 판별도 호스트가 아니라 경로(~/ABDAV) 기준으로 바꿨다.
호스트가 바뀌어도 같은 마운트를 찾아야 하기 때문이다.

## 사이드바 한 번에 열기는 불가능하다 (2026-09-11)

Finder 사이드바 위치 항목에서 한 번에 폴더가 열리게 만들려고 여러 가지를 시도했다.
결론은 **Finder 가 WebDAV 마운트를 무조건 서버 항목으로 표시한다**는 것이다.
서버를 누르면 그 안의 볼륨 목록이 나오고, 볼륨을 또 눌러야 파일이 보인다.

시도하고 실패한 것들.
- 마운트 지점을 홈 폴더로 옮김: 볼륨 이름은 바뀌지만 사이드바는 여전히 서버 표시
- 주소에서 경로를 빼고 뿌리째 마운트: 동일
- `mount_webdav -v` 로 볼륨 이름 지정: 창 제목만 바뀜
- 앱이 사이드바 즐겨찾기에 등록: LSSharedFileList 가 macOS 26 에서 크래시

남은 방법은 사용자가 ~/ABDAV 를 즐겨찾기로 한 번 끌어다 놓는 것뿐이다.
~/ABDAV 는 연결이 끊겨도 빈 폴더로 남으므로 즐겨찾기가 깨지지 않는다.

### 곁다리 정리
`--baseurl` 은 볼륨 이름을 바꾸려고 넣었던 건데, 이름이 마운트 폴더에서 온다는 게
밝혀지면서 쓸모가 없어졌다. 제거했다. 이름 변경 때 겪었던
"옛 baseurl 로 도는 서버" 문제의 원인도 같이 사라졌다.
